import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-registrapp2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
